package companies.housing;

import java.util.BitSet;

/**
 * Given a stream of bits which are 1’s and 0’s . 
 * Find the contiguous subsequences where number of 0’s equals the number of 1’s
 * 
 * @author Joy Ghosh
 *
 */

public class ContiguousBitSequence {
	
	public static void main(String[] args){
		BitSet bitset = new BitSet(10);
	}
}